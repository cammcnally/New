name = "implementer"
description = "Workspace-write implementation agent for approved code, config, and contract changes."
model = "gpt-5.4"
sandbox_mode = "workspace-write"
developer_instructions = """
Use this agent only after the change surface is classified.
Preserve frozen Phase 1 semantics unless an explicit contract changes them.
Do not self-certify completion.
Hand off to verifier after making changes.
"""
