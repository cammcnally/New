---
name: parallel-agent-handoff
description: Compatibility projection of the canonical `parallel-agent-handoff` skill registry entry from `AGENTS.md`.
---

This local skill file is non-canonical.

Primary inputs:

- `AGENTS.md`
- `.agents/skills/*`
- the Phase 1 governance docs under `docs/`

Local purpose:

- `Keep multiple agents aligned on shared contracts, outputs, and dependencies.`
