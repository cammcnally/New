---
name: strategy-report-bundle
description: Compatibility projection of the canonical `strategy-report-bundle` skill registry entry from `AGENTS.md`.
---

This local skill file is non-canonical.

Primary inputs:

- `AGENTS.md`
- `.agents/skills/*`
- the Phase 1 governance docs under `docs/`

Local purpose:

- `Produce reproducible, benchmark-aware report bundles for strategy runs.`
