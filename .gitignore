# Python
__pycache__/
*.py[cod]
*.so
.Python
.mypy_cache/
.venv/
venv/
env/
.local/
.env/
node_modules/

# IDE / editor
# Ignore .cursor contents except tracked projection paths (cannot re-include children if `.cursor/` is ignored).
.cursor/*
!.cursor/rules/
!.cursor/rules/**
!.cursor/agents/
!.cursor/agents/**
.idea/
*.swp
*.swo

# Overnight agent attempt payloads (local only)
ops/overnight/out/

# Pipeline run outputs (regenerable)
pipeline_outputs/
pipeline_outputs_*/

# Local relocated Python package bundle
python312_user_relocated/

# Ad-hoc / backup virtualenvs (canonical is .venv/)
.venv311/
.venv-py312-backup/

# Drive sync / temp upload debris
.tmp.driveupload/

# Local bundle notes (not repo documentation)
BUNDLE_README.md
BUNDLE_README.txt

# DVC
*.dvc
.dvc/config.local
.dvc/tmp
.dvc/cache

# MLflow
mlruns/

# Lineage events
lineage_events/

# Market data ingestion
data_lake/
*.duckdb
*.duckdb.wal
.env

# Cleanup audit outputs
outputs/
!outputs/.gitkeep

# Cursor skills/commands (generated, tracked separately)
!.cursor/skills/
!.cursor/skills/**
!.cursor/commands/
!.cursor/commands/**
!.cursor/settings.json
!.cursor/projection_manifest.json
!.cursor/environment.json
!.cursor/install.sh
!.cursor/start.sh
!.cursor/terminal.sh
