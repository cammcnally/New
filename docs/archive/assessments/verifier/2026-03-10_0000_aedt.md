# Verifier Assessment: Consolidation to pipeline_outputs (Option A)
> **SUPERSEDED**  
> This document is historical and non-authoritative.  
> Current file: `docs/assessments/verifier-latest.md`

# Assessment Metadata

- Assessment Type: verifier_assessment
- Status: SUPERSEDED
- Assessed At: 2026-03-10 00:00 AEDT
- Assessed From Commit: not-recorded-pre-lifecycle
- Assessed From Branch: unknown-pre-lifecycle
- Scope: Historical verification of the `pipeline_outputs` consolidation
- Supersedes: none
- Superseded By: docs/assessments/verifier-latest.md
- Authority Level: advisory

Original assessment did not record a precise timestamp or commit; the archive timestamp was normalized during lifecycle migration.

**Date:** 2026-03-10  
**Scope:** Validate consolidation from pipeline_outputs_smoke to pipeline_outputs; verify docs/config consistency and no broken references.

---

## Summary

**Status: PASS** — Consolidation is valid. Two remaining `pipeline_outputs_smoke` references in `.cursor/agents/pipeline-watcher.md` were fixed during verification. All docs/config now consistently direct to `pipeline_outputs`. No broken references remain in docs/config.

---

## (a) References to pipeline_outputs_smoke removed from docs/config

| Location | Status |
|----------|--------|
| README.md | ✓ No smoke-specific output_dir; uses `pipeline_outputs` for all runs |
| .cursor/commands/run-pipeline.md | ✓ `pipeline_outputs` default; no smoke dir |
| .cursor/skills/pipeline-runner-recovery/SKILL.md | ✓ `pipeline_outputs` for baseline and validation |
| .cursor/rules/output-and-resume-contract.mdc | ✓ Common values: `pipeline_outputs`, `pipeline_outputs_optuna` only |
| .cursor/rules/invoke-watcher-on-pipeline-failure.mdc | ✓ Example uses `pipeline_outputs` |
| .cursor/agents/pipeline-watcher.md | ✓ **Fixed during verification** — removed `pipeline_outputs_smoke` from examples (lines 12, 35) |
| subagent/path_consistency_assessment.md | ✓ **Updated during verification** — removed stale smoke wording from "Changes made" section |

**Remaining reference (expected):** `pipeline_outputs_smoke/06_state/config_snapshot.json` — contains `"output_dir": "pipeline_outputs_smoke"`. This file lives inside the folder the user will manually delete. No action needed; it will be removed with the folder.

---

## (b) run-pipeline and pipeline-runner-recovery direct to pipeline_outputs

| File | Verification |
|------|---------------|
| **run-pipeline.md** | ✓ Lists `pipeline_outputs` as default for all runs (baseline, smoke, validation). `pipeline_outputs_optuna` for Optuna. Recovery loop uses `{output_dir}` placeholder. |
| **pipeline-runner-recovery SKILL.md** | ✓ Baseline command: `--output_dir pipeline_outputs`. Validation runs use same. Optuna uses `pipeline_outputs_optuna`. Path authority and diagnostic paths reference `{output_dir}`. |

Both correctly direct baseline and validation runs to `pipeline_outputs`.

---

## (c) No broken references remain

- **Docs/config:** No references to `pipeline_outputs_smoke` in any doc or config file (after fixes).
- **Pipeline.py:** Uses `--output_dir` argument; no hardcoded output dir names.
- **Output tree:** `pipeline_outputs` exists with expected structure (`00_logs/`, `01_data/`, `02_metrics/`, etc.) and contains valid run data (log shows FRESH RUN START, panel verification).
- **Path authority:** `.cursor/rules/output-and-resume-contract.mdc` is the single source of truth; all references align.

---

## Fixes applied during verification

1. **.cursor/agents/pipeline-watcher.md**
   - Line 12: Removed `pipeline_outputs_smoke` from example; now `(e.g. pipeline_outputs)`.
   - Line 35: Replaced `e.g. pipeline_outputs_smoke for smoke runs` with `e.g. pipeline_outputs`.

2. **subagent/path_consistency_assessment.md**
   - Item 3: Removed "smoke test command"; added "consolidated to pipeline_outputs (smoke removed)".
   - Item 6: Replaced "Added smoke test section" with "Consolidated to pipeline_outputs".

---

## Action for user

**pipeline_outputs_smoke deletion:** The folder could not be deleted because `pipeline.log` is locked (file in use). Close the file in your IDE, then manually delete `pipeline_outputs_smoke`. Until then, the only remaining `pipeline_outputs_smoke` reference is inside that folder (`06_state/config_snapshot.json`), which is expected.

---

## Conclusion

Consolidation to `pipeline_outputs` (Option A) is complete and consistent. Docs, commands, skills, and rules all direct to `pipeline_outputs` for baseline and validation runs. No broken references remain in the repo.
